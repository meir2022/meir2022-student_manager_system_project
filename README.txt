this program aims to manage the database implemented on a txt file. 
print the valid object in the format set.
and give the user the option to add and filter the data.
is implemented with a linked list to avoid space limit as much as can.